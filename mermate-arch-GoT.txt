At the **absolute center** of the whole diagram sits one dominant master node called:

**HPC-GoT Canonical Controller**

This central node should be understood as the **core orchestrator**, the **governing reasoning engine**, and the **canonical MERMATE control layer for architectural system-specification generation**.

From that central controller, the diagram should radiate into eight major semantic regions:

1. **Purpose and Scope**
2. **Input Domain and Problem Model**
3. **Axiomatic Foundation**
4. **Formal State and Scoring Core**
5. **Operator Pipeline**
6. **Control Guarantees and Theorems**
7. **Implementation and Runtime Stack**
8. **Evaluation, Historical Variants, and Boundaries**

The most natural visual reading order is top-to-bottom and inside-out:
**Purpose first, then formal foundations, then execution flow, then guarantees, then implementation, then evaluation and limitations.**

---

## 1. Purpose and Scope region

Above the central controller, place a large parent region called:

**Architectural System-Specification Objective**

This region should be connected downward into the central HPC-GoT node with the relationship phrase:

**defines the mission of**

Inside this Purpose region, place five sibling nodes:

* **Mermaid Diagram Generation**
* **PlantUML and C4 Artifact Generation**
* **TLA+ Module and Bounded Model-Checking Scaffold Generation**
* **Security Architecture Specification**
* **Observability Design Specification**

These five nodes should all connect upward into the parent Objective node with the phrase:

**is a target artifact family within**

The parent Objective node should also connect into a node called:

**Search-and-Validate Problem Framing**

with the phrase:

**is formalized as**

That Search-and-Validate node should connect directly into the central controller with:

**motivates the design of**

Semantically, this top region says:

The paper is about architectural specification tasks, those tasks are framed as search-and-validate tasks, and that framing gives rise to HPC-GoT.

You should also place, adjacent to this region, a contextual support node called:

**Local 21B-Scale MoE Inference Setting**

This should connect into the central controller with:

**constrains the deployment of**

Inside that local inference setting, place subnodes:

* **gpt-oss-20b**
* **21B total parameters**
* **3.6B active parameters per token**
* **up to 128k context**
* **single-agent local inference**
* **consumer-hardware practicality goal**

These all connect into the Local Inference Setting node with:

**characterizes**

Then the Local Inference Setting node connects to HPC-GoT with:

**motivates bounded structured control for**

---

## 2. Input Domain and Problem Model region

To the upper-left of the controller, create a region called:

**Problem Formalization**

This region should connect to the central controller with:

**supplies the formal input contract for**

Inside it, define one parent node:

**Specification Instance**

This should connect to three child nodes:

* **Q: natural-language specification request**
* **I: finite invariant set**
* **T: target artifact type**

Each child connects upward to Specification Instance with:

**is a component of**

Then the Specification Instance node connects to HPC-GoT with:

**is consumed by**

Near this, create another parent node called:

**Artifact Space**

This should have children:

* **Artifact space for Mermaid**
* **Artifact space for PlantUML/C4**
* **Artifact space for TLA+**
* **Artifact space for security artifacts**
* **Artifact space for observability artifacts**

Each child connects to Artifact Space with:

**instantiates**

Artifact Space then connects to Problem Formalization with:

**provides the output universe for**

Also inside this region, define:

**Invariant Set**

This node should connect to:

* **Deterministic predicate**
* **Required-element predicate**
* **Task-specific constraint predicate**

using:

**contains**

Then Invariant Set connects to the central controller with:

**governs the validity assessment inside**

This is important semantically:
Use the phrase **required-element predicates are integrated into the invariant set** rather than saying completeness lives elsewhere. That phrase matters because later it replaces the older completeness heuristic.

---

## 3. Axiomatic Foundation region

Below the Problem Formalization region and to the left of the controller, create a strong formal block called:

**Axiomatic Framework**

This region should connect into the central HPC-GoT node with:

**grounds the formal correctness claims of**

Inside the Axiomatic Framework, organize three stacked subregions:

* **Artifact and Validation Axioms**
* **State and Resource Axioms**
* **Control and Output Axioms**

These should connect upward into Axiomatic Framework with:

**partitions**

### Artifact and Validation Axioms

Inside this subregion place:

* **Typed Artifact Domain Axiom**
* **Deterministic Invariants Axiom**
* **Required-Element Inclusion Axiom**
* **External Validator Determinism Axiom**

Each should connect upward with:

**is a member of**

Then this whole subregion should connect to Problem Formalization with:

**formalizes**

### State and Resource Axioms

Inside this subregion place:

* **Typed Controller State Axiom**
* **Global Budget Axiom**
* **Depth Cap Axiom**
* **Bounded Branching Axiom**

These connect upward with:

**is a member of**

This subregion should connect directly to the controller with:

**defines the structural constraints of**

### Control and Output Axioms

Inside this subregion place:

* **Threshold Pruning Axiom**
* **Single Optional Merge Axiom**
* **Batching Contract Axiom**
* **Formal Independence from Neuroscientific Premises Axiom**

This subregion should connect to the controller with:

**defines the execution discipline of**

This area should visually read like the formal constitution of the controller.

The semantic keywords that matter most here are:

* **grounds**
* **formalizes**
* **constrains**
* **defines**
* **governs**
* **partitions**
* **instantiates**

---

## 4. Formal State and Scoring Core region

Directly beneath the central HPC-GoT controller, place a wide region called:

**Formal Core**

This region should connect into the controller with:

**implements the internal semantics of**

Inside the Formal Core, place two major sibling blocks:

* **Typed State Model**
* **Canonical Validity Score**

### Typed State Model

This should be a parent node called:

**Controller State Tuple**

with seven child nodes:

* **theta: internal thought representation**
* **ell: level index**
* **A: current artifact**
* **sigma: current validity score**
* **p: parent-state identifier**
* **F: validator failure trace**
* **pi: provenance map**

Each child connects upward with:

**is a field of**

This state tuple node connects to HPC-GoT with:

**is the fundamental unit manipulated by**

Then create another node called:

**Global Controller-State Budget N = 40**

This connects to Controller State Tuple with:

**bounds the total number of instances of**

And it also connects directly to HPC-GoT with:

**globally constrains**

Then create a node called:

**Complete 3-ary depth-3 tree interpretation**

with child nodes:

* **level 0 = 1 node**
* **level 1 = 3 nodes**
* **level 2 = 9 nodes**
* **level 3 = 27 nodes**
* **total = 40 nodes**

This subtree connects upward with:

**explains the canonical budget value of**

That explanatory-tree node then connects to the Global Budget node with:

**justifies**

### Canonical Validity Score

Next to the state model, place a parent node:

**Canonical Score Function**

This should connect downward into two equal children:

* **Structural Validity**
* **Invariant Coverage**

Each child connects upward with:

**is a weighted component of**

You should explicitly use the wording:
**is weighted equally within**
because the score is half structural validity and half invariant coverage.

Then add a side-note node called:

**Required-element coverage is absorbed into the invariant layer**

This should connect to Invariant Coverage with:

**refines**

Then create another side-note node:

**Completeness-by-length term removed from canonical design**

This should connect to Canonical Score Function with:

**is excluded from**

And connect that removed-completeness node to a historical node called:

**Legacy score with completeness heuristic**

using:

**originates from**

That historical node should live later in the Historical Variants section, but here you can hint at it with a dotted semantic notion like:
**has historical predecessor in**

The critical wording here is:

* **fundamental unit**
* **weighted component**
* **globally constrains**
* **justifies**
* **refines**
* **is excluded from**
* **has historical predecessor in**

---

## 5. Operator Pipeline region

To the right of the controller, create the most important execution-flow region called:

**Controller Operator Algebra**

This should connect to HPC-GoT with:

**drives the execution of**

Inside it, create a strict linear sequence of operator nodes, but also keep them nested under one operator-governance parent.

The parent should be:

**Canonical Control Pipeline**

Children, in exact order:

1. **Initialize Root State**
2. **Branch**
3. **Decompose**
4. **Validate**
5. **Prune**
6. **Select**
7. **Optional Merge**
8. **Return Final Artifact**

Each should connect to the next with the phrase:

**feeds into**

And each operator should also connect upward to Canonical Control Pipeline with:

**is a stage within**

### Initialize Root State

This first node should connect back to Specification Instance with:

**instantiates the root from**

It should also connect to Controller State Tuple with:

**creates the first instance of**

### Branch

The Branch node should connect to:

* **at most 3 child states**
* **distinct thought representations**
* **budget guard**
* **level increment**

using:

* **produces**
* **requires**
* **advances**

Branch should also connect to Global Budget with:

**is the only operator that consumes state-creation budget from**

This is very important language.

### Decompose

Decompose should connect to:

* **target-format artifact generation**
* **Mermaid artifact**
* **PlantUML/C4 artifact**
* **TLA+ artifact**
* **security artifact**
* **observability artifact**

using:

**materializes into**

### Validate

Validate should connect to:

* **Router-Side Validator**
* **Structural Validity result**
* **Failure Trace**
* **Provenance Extraction**
* **Score Computation**

with:

**invokes**
**computes**
**records**
**derives**

The Router-Side Validator node itself should connect outward to several validator tools:

* **Mermaid CLI**
* **PlantUML renderer**
* **C4-PlantUML conventions**
* **SANY parser**
* **TLC bounded checker**
* **deterministic security predicate checks**
* **observability predicate checks**

using:

**is instantiated by**

### Prune

Prune should connect to:

* **threshold tau = 0.85**
* **retained candidate set**
* **discarded sub-threshold candidates**

using:

**applies**
**retains**
**eliminates**

Then Prune should connect to the theorem region later with:

**satisfies the stage-local pruning property proved in**

### Select

Select should connect to:

* **highest sigma leaf**
* **tie-break by smaller failure trace**
* **surviving frontier**

using:

**chooses**
**resolves ties by**
**operates over**

### Optional Merge

This node should sit after Select but also take inputs from the top terminal leaves. So semantically it is not only linear; it is also aggregative.

Optional Merge should connect to:

* **top terminal leaves**
* **best artifact as merge anchor**
* **element extraction**
* **conflict predicate**
* **compatibility filter**
* **provenance-preserving rewrite**
* **non-decreasing score acceptance rule**

using:

**aggregates**
**anchors on**
**requires**
**filters through**
**executes as**
**is accepted under**

Then Optional Merge connects to Return Final Artifact with:

**may replace the selected artifact before**

### Return Final Artifact

This final node connects to:

* **final artifact**
* **final score**
* **audit metadata**
* **optional provenance map**

using:

**outputs**
**includes**

The crucial action words for this whole operator pipeline are:

* **creates**
* **consumes**
* **materializes**
* **invokes**
* **computes**
* **records**
* **derives**
* **retains**
* **eliminates**
* **chooses**
* **aggregates**
* **filters**
* **rewrites**
* **outputs**

---

## 6. Merge and provenance sub-architecture

Below the Operator Pipeline, make a nested technical region called:

**Merge, Conflict Algebra, and Provenance Layer**

This region should connect to Optional Merge with:

**supplies the formal semantics for**

Inside it, arrange a four-step chain:

1. **Element Extraction**
2. **Conflict Predicate**
3. **Compatibility Lift**
4. **Merge Construction**
5. **Merge Acceptance Rule**
6. **Provenance Recording**

### Element Extraction

This node should connect from each artifact type with:

**parses into typed atomic elements from**

### Conflict Predicate

This node should connect to element pairs with:

**tests incompatibility between**

Its child semantic conditions should be:

* **identifier clash**
* **name clash**
* **interface mismatch**
* **duplicate key**
* **incompatible declaration**

All connect upward with:

**is an example of**

### Compatibility Lift

This node should connect from Conflict Predicate with:

**lifts into artifact-level compatibility from**

And it should connect to Merge Construction with:

**filters candidate elements for**

### Merge Construction

This node should connect to:

* **best artifact**
* **compatible elements from alternative artifacts**
* **union-style rewrite**
* **single terminal state slot**

using:

**starts from**
**adds**
**constructs**
**rewrites within**

### Merge Acceptance Rule

This node should connect to:

* **merged score**
* **maximum source score**

using:

**compares**
**accepts only when**
**rejects otherwise**

### Provenance Recording

This should connect to Merge Construction with:

**annotates the output of**

and connect to later audit functionality with:

**enables**

This layer is the “semantic repair algebra” of the whole design.

---

## 7. Guarantee and theorem region

Below the Formal Core and Operator Pipeline, create a large formal region called:

**Controller-Level Guarantees**

This should connect upward to HPC-GoT with:

**proves bounded properties of**

Inside it, place six theorem nodes:

* **Termination Theorem**
* **Global State Bound Theorem**
* **Default Complete-Tree Upper-Bound Proposition**
* **Batched LLM-Stage Bound Theorem**
* **Stage-Local Pruning Property Theorem**
* **Merge Non-Expansion Theorem**

Each connects upward with:

**is a formal guarantee within**

Then connect each theorem back to the operator or axiom it depends on.

### Termination Theorem

Connect to:

* **Depth Cap Axiom**
* **Canonical Schedule**

using:

**follows from**

### Global State Bound Theorem

Connect to:

* **Global Budget Axiom**
* **Branch as sole state-creating operator**

using:

**follows from**

### Complete-Tree Upper-Bound Proposition

Connect to:

* **Bounded Branching Axiom**
* **Depth Cap Axiom**
* **1 + 3 + 9 + 27 structure**

using:

**is derived from**

### Batched LLM-Stage Bound Theorem

Connect to:

* **Batching Contract Axiom**
* **three level transitions**
* **optional single merge stage**

using:

**depends on**
**counts**
**adds**

### Stage-Local Pruning Theorem

Connect to:

* **Threshold Pruning Axiom**
* **retained candidate set**
* **max score before pruning**
* **max score after pruning**

using:

**formalizes**
**compares**
**preserves under survivor-maximizer condition**

### Merge Non-Expansion Theorem

Connect to:

* **Single Optional Merge Axiom**
* **rewrite semantics**
* **no new state creation**

using:

**follows from**

This theorem region should feel like the mathematical certification layer of the architecture.

---

## 8. Implementation and runtime stack region

To the far right, build a vertical layered stack called:

**MERMATE Local Runtime Architecture**

This should connect to the central controller with:

**deploys**

Inside this runtime architecture, create three stacked layers:

1. **Controller Layer**
2. **Prompt Compiler Layer**
3. **Validator Layer**

They should connect top-to-bottom with:

**hands off to**

### Controller Layer

This should include:

* **state management**
* **budget accounting**
* **frontier management**
* **pruning control**
* **merge orchestration**
* **selection logic**
* **provenance tracking**

Each connects upward with:

**is handled by**

### Prompt Compiler Layer

This should include:

* **Harmony format enforcement**
* **batched schema construction**
* **JSON structured output contract**
* **repair-or-reask fallback**
* **strict output normalization**

Each connects upward with:

**is handled by**

This layer should connect to the LLM runtime node called:

**Local gpt-oss-20b inference through Ollama**

with:

**mediates calls to**

Then that local runtime node should connect to supporting infrastructure nodes:

* **Harmony response format**
* **Ollama local serving**
* **consumer GPU deployment**
* **MXFP4-supporting kernels**
* **open-weight MoE execution**

using:

**depends on**
**is served by**
**targets**
**is accelerated by**
**runs as**

### Validator Layer

This should include:

* **Mermaid CLI harness**
* **PlantUML harness**
* **TLA+ harness**
* **security predicate harness**
* **observability predicate harness**
* **timeout wrapper**
* **failure trace formatter**

Each connects upward with:

**is a component of**

Then Validator Layer connects back into Controller Layer with:

**returns scores and diagnostics to**

That return edge is semantically very important.

---

## 9. Failure trace and feedback region

Near the runtime stack, create a smaller but important loop called:

**Validation Feedback Loop**

This should connect from Validator Layer back to Decompose with:

**feeds structured failure traces into subsequent generations for**

Then inside it place:

* **structured negatives**
* **diagnostic feedback**
* **debug metadata**
* **developer-facing internal reasoning traces**

Each connects upward with:

**is a form of**

Then this feedback loop should connect to MERMATE developer tooling with:

**supports debugging and refinement in**

This area should not be overstated as theorem-backed. It is implementation-support architecture.

---

## 10. Evaluation and benchmarking region

Below the runtime stack, create a region called:

**Experimental Program**

This should connect to HPC-GoT with:

**evaluates**

Inside it create five children:

* **Benchmark Families**
* **Baselines**
* **Metrics**
* **Statistical Protocol**
* **Experimental Timeline**

### Benchmark Families

This should contain:

* **C4 multi-view systems**
* **Mermaid architectural diagrams**
* **TLA+ safety and liveness scaffolds**
* **Security architecture specifications**
* **Observability designs**

Each connects upward with:

**is included in**

### Baselines

This should contain:

* **Plain CoT**
* **Self-Consistency**
* **Tree-of-Thoughts**
* **Graph-of-Thoughts**
* **Breadth ablations of HPC-GoT**
* **Forest-of-Thought**
* **Plan-of-Thought**
* **Adaptive Graph of Thoughts**
* **Policy-guided bounded-query ToT**

Each connects upward with:

**is compared against**

### Metrics

This should split into two subgroups:

**Primary automated metrics**

* validator pass rate
* mean invariant coverage
* mean sigma

**Secondary metrics**

* contradiction rate
* token usage
* peak context length
* wall-clock latency
* blinded human architectural assessment

These subgroup nodes connect upward with:

**partitions**

And the Metrics parent connects to Experimental Program with:

**measures the outcomes of**

### Statistical Protocol

This should contain:

* **five random seeds**
* **mean plus 95 percent confidence interval**
* **paired testing where appropriate**

These connect upward with:

**specifies**

### Experimental Timeline

This should contain a phased structure:

* **Build**
* **Pilot**
* **Full experiments**
* **Write-up**

And each phase should contain its substeps:

* Validator harness
* MERMATE router skeleton plus Harmony schema
* Dataset generator plus invariant templates
* Pilot run
* Failure-mode fixes plus instrumentation
* Full run
* Analysis plus ablations plus CI reports
* Final arXiv draft plus artifacts plus code release

Connect these with:
**precedes**
**feeds into**
**culminates in**

---

## 11. Historical variants and non-canonical region

To the lower-left, create a region called:

**Historical Variants and Developmental Record**

This should connect to the main controller with:

**documents prior formulations of**

Inside it place:

* **Legacy completeness-based score**
* **Optional non-batched 53-call upper bound**
* **Adaptive breadth extension**
* **Adaptive threshold extension**
* **Stronger neuro-mathematical framings**

Each should connect upward with:

**is a historical variant of**

Then connect each one back to the canonical region with the right semantic relationship:

* Legacy completeness-based score → Canonical Score Function with
  **was superseded by**

* Non-batched 53-call upper bound → Batched LLM-Stage Bound Theorem with
  **is a conservative schedule-dependent counterpart to**

* Adaptive breadth extension → Canonical Schedule with
  **remains an optional extension to**

* Adaptive threshold extension → Threshold Pruning Axiom with
  **was excluded from the canonical formulation of**

* Stronger neuro-mathematical framings → Formal Independence from Neuroscientific Premises Axiom with
  **was demoted beneath**

This region is extremely important if you later want the diagram to show evolution rather than only the final state.

---

## 12. Neural-framing boundary region

Create a clean conceptual boundary box called:

**Neural and Psychological Inspiration Boundary**

This should connect to the main paper with:

**contextualizes but does not ground**

Inside it place three semantic categories:

* **Admissible loose structural inspiration**
* **Appendix-only biological analogy**
* **Removed mechanistic neuroscience claims**

Under the first category, place:

* **coarse-to-fine processing analogy**
* **competition analogy**
* **late integration analogy**

Under the second category, place:

* **cortical competition metaphor**
* **broadcast integration metaphor**

Under the third category, place:

* **thalamo-cortical gating**
* **anterior-cingulate pruning**
* **variational free energy**
* **Thousand Brains consensus**
* **predictive coding master formula**

Then connect those third-category nodes to the main formal core with:

**must not enter the formal proof layer of**

This semantic phrasing matters. It distinguishes inspiration from mechanism.

---

## 13. Limitations and ethics region

At the bottom of the whole architecture, place a concluding support region called:

**Boundaries, Limitations, and Responsible Use**

This should connect upward to HPC-GoT with:

**qualifies the claims of**

Inside it place:

* **controller-level guarantees only**
* **no claim of global optimality**
* **no claim of hallucination guarantees**
* **no claim of neuroscientific equivalence**
* **validator success is not semantic sufficiency**
* **TLC runtime variability**
* **runtime depends on full integration stack**
* **defensive security use emphasis**
* **offensive exploitation not provided**

Each connects upward with:

**is a limitation or guardrail within**

This lower region should make the entire diagram feel honest and publication-safe.

---

# How the whole diagram should feel structurally

If someone were drawing it later, the whole thing should feel like a **layered cathedral**:

* **Top crown:** Purpose, scope, deployment context
* **Upper-left:** Problem model and formal input domain
* **Upper-right:** Related-work positioning
* **Center:** HPC-GoT canonical controller
* **Directly beneath center:** Formal core of states and scoring
* **Right side:** operator pipeline and runtime stack
* **Left side:** axiomatic framework
* **Lower center:** theorem and guarantee layer
* **Lower-right:** evaluation program
* **Lower-left:** historical variants
* **Very bottom:** limitations and ethics
* **Outer conceptual boundary:** neural inspiration kept separate from formal mechanism

---

# Best semantic connector words to use later in Markdown or Mermaid

These are the exact kinds of words that will make the later diagram feel precise and architecturally intelligent rather than vague.

Use these for **containment**:

* contains
* includes
* consists of
* is composed of
* partitions into
* is a member of
* instantiates

Use these for **definition and formal grounding**:

* defines
* formalizes
* grounds
* specifies
* constrains
* governs
* certifies
* justifies

Use these for **execution flow**:

* initializes
* branches into
* decomposes into
* validates through
* derives
* prunes into
* selects from
* merges into
* outputs

Use these for **proof dependency**:

* follows from
* depends on
* is derived from
* is guaranteed by
* is bounded by
* is preserved under

Use these for **score semantics**:

* is weighted within
* contributes to
* is absorbed into
* is excluded from
* is computed by
* is determined by

Use these for **runtime relationships**:

* is served by
* is mediated by
* is enforced by
* is wrapped by
* is returned to
* is handled by
* is injected into

Use these for **historical evolution**:

* was superseded by
* was refined into
* was excluded from
* remains an optional extension to
* is a historical predecessor of
* was demoted beneath

Use these for **boundaries and caveats**:

* contextualizes but does not ground
* qualifies
* limits
* does not imply
* must not enter
* reduces risk under
* does not eliminate
