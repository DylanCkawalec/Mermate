\* TLC model configuration — MermateOrchestrator (control-plane design)
\* Run: java -jar tla2tools.jar -config MermateOrchestrator.cfg MermateOrchestrator.tla
\* Deadlock checking is safe to leave on: Reload is always enabled, so no
\* reachable state is a deadlock state.

CONSTANTS Stages = {"idea", "md", "mmd", "tla", "ts"}

SPECIFICATION Spec

INVARIANTS Inv

PROPERTIES Termination VerifyResponds
